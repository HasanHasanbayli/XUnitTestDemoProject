namespace Test;

public class ProductServiceTest
{
    // private readonly Mock<IProductService> _productServiceMock;
    // private readonly Fixture _fixture;

    // public ProductServiceTest()
    // {
    // _productServiceMock = new Mock<IProductService>();
    // _fixture = new Fixture();
    // }

    // [Fact]
    // public void Product_Get()
    // {
    // var product = _fixture.Create<Product>();

    // var productId = _fixture.Create<int>();

    // _productServiceMock.Setup(service => service.Get(productId)).Returns(product);

    // var actualProduct = _productServiceMock.Object.Get(productId);

    // Assert.True(product.Equals(actualProduct));
    // }

    // [Fact]
    // public void Product_GetAll()
    // {
    // var products = _fixture.Create<List<Product>>();

    // _productServiceMock.Setup(service => service.GetAll()).Returns(products);

    // var actualProducts = _productServiceMock.Object.GetAll();

    // Assert.True(products.Equals(actualProducts));
    // }

    // [Fact]
    // public void Product_Add()
    // {
    // var productCreateDto = _fixture.Create<ProductCreateDto>();

    // _productServiceMock.Setup(service => service.Add(productCreateDto)).Returns(true);

    // var result = _productServiceMock.Object.Add(productCreateDto);

    // Assert.True(result);
    // }

    // [Fact]
    // public void Product_Update()
    // {
    // var productUpdateDto = _fixture.Create<ProductUpdateDto>();

    // _productServiceMock.Setup(service => service.Update(productUpdateDto)).Returns(true);

    // var result = _productServiceMock.Object.Update(productUpdateDto);

    // Assert.True(result);
    // }

    // [Fact]
    // public void Product_Delete()
    // {
    // var productId = _fixture.Create<int>();

    // _productServiceMock.Setup(service => service.Delete(productId)).Returns(true);

    // var result = _productServiceMock.Object.Delete(productId);

    // Assert.True(result);
    // }
}